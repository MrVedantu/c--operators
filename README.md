# C Operators ⚙️

This repository contains simple C programs demonstrating the use of various operators in the C programming language.

## 📘 Topics Covered
- Arithmetic Operators (`+`, `-`, `*`, `/`, `%`)
- Relational Operators (`==`, `!=`, `>`, `<`, `>=`, `<=`)
- Logical Operators (`&&`, `||`, `!`)
- Bitwise Operators (`&`, `|`, `^`, `<<`, `>>`)
- Assignment Operators (`=`, `+=`, `-=`, `*=`, `/=`, `%=`) 
- Increment and Decrement Operators (`++`, `--`)
- Conditional (Ternary) Operator (`?:`)

## 💻 Example
```c
#include <stdio.h>

int main() {
    int a = 10, b = 5;
    printf("Sum = %d\n", a + b);
    printf("Difference = %d\n", a - b);
    printf("Product = %d\n", a * b);
    printf("Division = %d\n", a / b);
    printf("Modulus = %d\n", a % b);
    return 0;
}
